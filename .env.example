# Server (server/index.js)
PORT=3001
CLIENT_PASSWORD=changeme
ADMIN_PASSWORD=changeme

# Set to true if Node should serve the built dist/ directly.
# Leave unset when fronting with nginx/Apache (recommended).
SERVE_STATIC=
